1.  mkdir called repo eg mkdir repo
    eg mkdir repo
 2. cd into the directory using cd "name of directory"
 3. clone your local repository with the remote one by using git clone "copy from remote"
 4 then cd into the projectuse vim to eddit your project file
 5. check your git status by using "git status"
 6. add the changes by using "git add gitjenkin-----"
 7. git commit by using git commit -m "message as desired"
 8. push by using "git push origin "then the branch name"
 9. then go to GitHub remote repository and make a pull request
 10. the pull request is toing to be matched by a senior devops engineer
 

