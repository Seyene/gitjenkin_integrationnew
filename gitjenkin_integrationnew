1.  mkdir called repo eg mkdir repo
    eg mkdir repo
 2. create a file and name it using touch
 3. add the file using git add "filename"
 4. clone your local repository with the remote one by using git clone "copy from remote"
 5 then cd into the projectuse vim to eddit your project file
 check your git status by using "git status"
 add the changes by using "git add gitjenkin-----"
 git commit by using git commit -m "message as desired"
 push by sing "git push origin master"
 

